# DoorDash imports use the Chrome extension (see /extension in the repo).

# --- AI Enhancement (optional) ---
# Enables the "AI Enhance" button in the menu builder.
# Get a key at https://console.anthropic.com/
# NOTE: This key is included in the browser bundle, so treat it as a
# restricted key (Anthropic lets you set per-key usage limits).
# VITE_ANTHROPIC_API_KEY=sk-ant-...
