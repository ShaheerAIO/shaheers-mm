/**
 * Pixel width for each stacked `RightSidebar` panel.
 * ~10% wider than the original 320px design (352px), then ~10% again for comfortable mod creation.
 */
export const RIGHT_PANEL_WIDTH_PX = 388;
